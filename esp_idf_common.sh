# Used by scripts in this directory to find the cloned repo path
export ESP_IDF_DIR="$(pwd)/esp-idf"
# Used by the esp-idf tools to keep installs local to this directory.
export IDF_TOOLS_PATH="${ESP_IDF_DIR}/install"
